// Ce code a été généré par une IA
document.querySelectorAll('.hoverconsole').forEach(function(element) {
    element.addEventListener('mouseover', function() {
        console.log(element.textContent);
    });
});
// Fin du code généré par l'IA

console.log("Test")